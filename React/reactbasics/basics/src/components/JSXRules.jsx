import React from 'react'

export const JSXRules = () => {
  return (
    <div>
        <h1>Rules of JSX</h1>
        <p>JSX elements must be property closed</p>
        <p>JSX must return a single parent element</p>
        <p> JSX attributes are written using camelCase</p>
        <p>tip:Use label for input</p>
    </div>
  )
}
